# Technology Stack

> Auto-generated by /map on 2026-02-13

## Runtime

| Technology | Version | Purpose |
|------------|---------|---------|
| Python | 3.8+ (required) | Core runtime |
| Tkinter | stdlib | GUI framework |

## Dependencies

### Production
| Package | Version | Purpose |
|---------|---------|---------|
| Pillow (PIL) | Any recent | Image I/O, resize, crop, format conversion |
| tkinter | stdlib | GUI — windows, canvas, widgets, dialogs |
| json | stdlib | Project file serialization (.lab format) |
| os | stdlib | File path operations |
| platform | stdlib | OS detection (macOS vs Windows) |
| subprocess | stdlib | macOS dark mode detection |

### Development
| Package | Version | Purpose |
|---------|---------|---------|
| *(none)* | — | No dev dependencies configured |

## Infrastructure

| Service | Provider | Purpose |
|---------|----------|---------|
| Local filesystem | OS | Image loading, project save/load, tile export |

*Fully offline — no cloud services, APIs, or databases.*

## Configuration

| Variable | Purpose | Location |
|----------|---------|----------|
| `Image.MAX_IMAGE_PIXELS` | Set to `None` to bypass Pillow's decompression bomb limit | `main.py:9` |
| `EXPORT_FORMATS` | Supported export formats (PNG/JPEG/TIFF/BMP/WebP) | `main.py:65-71` |
| LOD max size (`2048`) | Max preview dimension for LOD cache | `main.py:48` |
| Default grid size (`1000x1000`) | Initial grid cell size in pixels | `main.py:41-42` |
| Autosave delay (`2000ms`) | Debounce timer for autosave after modification | `main.py:405` |
| Window size (`1400x900`) | Default window geometry | `main.py:79` |

## Outdated Packages

No package manager (no `requirements.txt`, `pyproject.toml`, or `setup.py`). Dependencies are installed manually via `pip install Pillow`.

| Package | Current | Latest | Risk |
|---------|---------|--------|------|
| Pillow | Unknown | — | Low (only dependency) |

## Missing Infrastructure

- No `requirements.txt` or `pyproject.toml` for reproducible installs
- No virtual environment configuration
- No CI/CD pipeline
- No test framework
- No linter/formatter configuration
