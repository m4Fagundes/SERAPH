---
phase: 1
plan: 1
title: "Remove duplicate * 2.py files from git"
wave: 1
depends_on: []
files_modified:
  - "app/application/interactive_segmentation_service 2.py"
  - "app/domain/interfaces/segmentation_model 2.py"
  - "app/infrastructure/analyzers/__init__ 2.py"
  - "app/infrastructure/analyzers/dummy_analyzer 2.py"
  - "app/infrastructure/ml_models/nuclick_adapter 2.py"
  - "app/infrastructure/ml_models/nuclick_torch/__init__ 2.py"
  - "app/infrastructure/ml_models/nuclick_torch/architecture 2.py"
  - "app/infrastructure/ml_models/nuclick_torch/guiding_signals 2.py"
  - "app/infrastructure/ml_models/nuclick_torch/layers 2.py"
  - "app/infrastructure/ml_models/nuclick_torch/process 2.py"
autonomous: true
requirements: [DEP-04]
must_haves:
  truths:
    - "No file matching '* 2.py' exists in the git-tracked tree"
    - "The canonical originals (without ' 2' suffix) still exist and are unmodified"
  artifacts:
    - path: "app/infrastructure/ml_models/nuclick_adapter.py"
      provides: "Canonical NuClick adapter — must survive intact"
    - path: "app/infrastructure/ml_models/nuclick_torch/architecture.py"
      provides: "Canonical NuClick architecture — must survive intact"
  key_links:
    - from: "git index"
      to: "app/**/*.py"
      via: "git ls-files"
      pattern: " 2\\.py$"
---

<objective>
Remove all ten `* 2.py` duplicate files from version control using `git rm`.

Purpose: These stale copies (e.g., `nuclick_adapter 2.py`) are git-tracked but unreachable via Python imports (spaces in filenames are invalid module identifiers). They will inflate the PyInstaller `.app` bundle and may confuse `collect_all('app')` in Phase 2. DEP-04 requires their removal.

Output: Ten files removed from git index and filesystem. Canonical originals untouched. One commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planing/PROJECT.md
@.planing/ROADMAP.md
</context>

<tasks>

<task id="T1" type="execute">
  <title>git rm all ten duplicate files and commit</title>
  <read_first>
    - Run `git ls-files | grep " 2\.py$"` to confirm the exact 10 tracked paths before deleting anything.
    - Run `git ls-files | grep -v " 2\.py$" | grep "nuclick_adapter\|architecture\|guiding_signals\|layers\|process\|interactive_segmentation\|segmentation_model\|analyzers"` to confirm all originals are still tracked.
  </read_first>
  <action>
    Execute the following `git rm` commands (one per file — paths contain spaces, so quote each):

    ```
    git rm "app/application/interactive_segmentation_service 2.py"
    git rm "app/domain/interfaces/segmentation_model 2.py"
    git rm "app/infrastructure/analyzers/__init__ 2.py"
    git rm "app/infrastructure/analyzers/dummy_analyzer 2.py"
    git rm "app/infrastructure/ml_models/nuclick_adapter 2.py"
    git rm "app/infrastructure/ml_models/nuclick_torch/__init__ 2.py"
    git rm "app/infrastructure/ml_models/nuclick_torch/architecture 2.py"
    git rm "app/infrastructure/ml_models/nuclick_torch/guiding_signals 2.py"
    git rm "app/infrastructure/ml_models/nuclick_torch/layers 2.py"
    git rm "app/infrastructure/ml_models/nuclick_torch/process 2.py"
    ```

    Then commit:
    ```
    git commit -m "chore: remove 10 duplicate '* 2.py' files (DEP-04)"
    ```

    Do NOT use `git rm -r app/` or any glob that could catch canonical files.
    Do NOT use OS-level delete (rm/del) — must go through `git rm` to remove from index.
  </action>
  <acceptance_criteria>
    - `git ls-files | grep " 2\.py$"` returns empty (exit code 1 or zero lines)
    - `git ls-files app/infrastructure/ml_models/nuclick_adapter.py` returns exactly `app/infrastructure/ml_models/nuclick_adapter.py` (canonical file untouched)
    - `git ls-files app/infrastructure/ml_models/nuclick_torch/architecture.py` returns `app/infrastructure/ml_models/nuclick_torch/architecture.py`
    - `git log --oneline -1` shows the DEP-04 commit message
    - `git status` shows a clean working tree (no unstaged changes)
  </acceptance_criteria>
</task>

</tasks>

<verification>
After the task completes, run these checks from the repo root:

```bash
# Must return 0 lines
git ls-files | grep " 2\.py$"

# Must return the canonical file (not deleted)
git ls-files app/infrastructure/ml_models/nuclick_adapter.py

# Must show clean tree
git status
```
</verification>

<must_haves>
  <truths>
    - No file matching `* 2.py` pattern is tracked by git
    - All 10 canonical originals (nuclick_adapter.py, architecture.py, guiding_signals.py, layers.py, process.py, interactive_segmentation_service.py, segmentation_model.py, analyzers/__init__.py, dummy_analyzer.py, nuclick_torch/__init__.py) remain intact and tracked
    - Commit exists referencing DEP-04
  </truths>
</must_haves>

<output>
After completion, create `.planing/phases/01-runtime-foundation/01-1-SUMMARY.md` with:
- What was done (which 10 files removed)
- Verification output (git ls-files grep result)
- Any surprises encountered
</output>
